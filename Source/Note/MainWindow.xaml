<Window
    x:Class="Note.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Note"
    xmlns:controls="using:Note.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid RowDefinitions="Auto,Auto,*">
        <Grid HorizontalAlignment="Stretch" Height="0.5" Name="AppTitleBar"/>
        <Grid ColumnDefinitions="Auto,*">
            <controls:Menu x:Name="_menu"/>
            <controls:WindowCaptionButtons Grid.Column="1" VerticalAlignment="Stretch"/>
        </Grid>
        <controls:TabBar x:Name="_tabs" Grid.Row="1" Grid.ColumnSpan="2">
            <controls:TabBar.Resources>
                <SolidColorBrush x:Key="TabViewItemIconForeground" Color="#B1B1B1" />
                <SolidColorBrush x:Key="TabViewItemHeaderForeground" Color="#B1B1B1" />
            </controls:TabBar.Resources>
        </controls:TabBar>
    </Grid>
</Window>

